# User Guide

1. Start the stack with `docker-compose up -d --build`.
2. Open `http://localhost:3000`.
3. Enter a GitHub owner and repository.
4. Review decisions, ghost-code candidates, scar tissue, ownership, bus-factor alerts, and onboarding steps.
5. Ask the Oracle focused questions such as "Why is this defensive retry here?" or "Who owns the API layer?"
